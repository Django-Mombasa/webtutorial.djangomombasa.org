# Request and response

When you visit `https://example.com`:

1. The browser finds the server for `example.com` using DNS.
2. It sends an **HTTP request** for the page.
3. The server sends back an **HTTP response** containing the page.
4. The browser turns that response into pixels on your screen.
