# Branches

```bash
git checkout -b feature/new-page
# ...make changes...
git add .
git commit -m "Add new page"
git push -u origin feature/new-page
```

Open a **pull request** on GitHub to propose merging your branch into
`main`.
