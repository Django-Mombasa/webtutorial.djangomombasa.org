# The basic workflow

```bash
git status              # see what changed
git add .               # stage changes
git commit -m "Message" # record a snapshot
```

A **commit** is a saved point in history. Write short, clear messages
that describe *why* you made the change.
