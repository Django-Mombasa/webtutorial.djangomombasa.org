# If, for, and functions

```python
if age >= 18:
    print("Adult")
else:
    print("Minor")

for fruit in fruits:
    print(fruit)

def greet(name):
    return f"Hello, {name}!"

print(greet("World"))
```

Indentation matters in Python — each block is defined by spaces (use 4).
