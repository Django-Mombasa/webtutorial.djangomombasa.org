# Selectors, properties, and values

A CSS rule has three parts:

```css
selector {
  property: value;
}
```

- **Selector** — which elements to style (`h1`, `.card`, `#main`).
- **Property** — what to change (`color`, `margin`, `font-size`).
- **Value** — the new setting (`red`, `10px`, `1.2em`).
