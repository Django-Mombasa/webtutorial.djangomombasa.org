# Tags and elements

HTML is made of **tags** wrapped in angle brackets. Most tags come in
pairs — an opening tag and a closing tag — with content in between:

```html
<p>This is a paragraph.</p>
```

Together, a tag pair and its content form an **element**.
