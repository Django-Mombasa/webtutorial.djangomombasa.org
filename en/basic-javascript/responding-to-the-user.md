# Responding to the user

```javascript
const button = document.querySelector("#say-hi");
button.addEventListener("click", () => {
  alert("Hi there!");
});
```

This finds the button with `id="say-hi"` and runs code when it is
clicked.
